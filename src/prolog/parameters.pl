% thresholds for edge_point gradient
edge_point_thresh(5).
% edge shrink size
edge_shrink_thresh(0.2).
% thresholds for same_line parameters difference
same_line_para_thresh(0.015).
% threshold for "3 points on same line"
on_seg_thresh(0.03).
% threshold for edge subsumption
edge_subsume_thresh(0.05).
% point near threshold
point_near_thresh(0.015).
% edge near threshold
edge_near_thresh(0.02).
% combo minimum dist threshold
combo_min_dist_thresh(0.015).
% inside combo dist threshold
combo_max_dist_thresh(0.5).
% recursion limit for edge_line testing
recursion_limit(4).
% threshhold of edge point proportion
edge_points_proportion_threshold(1.0).
% new point sampling times limit
sample_new_point_time_limit(10).
% new point sampling times limit
sample_new_point_size_limit(10).
% edge sampling time limit
sample_edge_limit(10000).
